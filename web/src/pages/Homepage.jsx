

export default Homepage = () => {
    //once user logs in, they see this page. 
    /* 
        links to: 
        check order status, (OrderStatus)
        see previous orders, (OrderStatus)
        write reviews, (WriteReview)
        request refund, (Contact)

    */
}